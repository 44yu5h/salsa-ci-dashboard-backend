// We calculate the hourly stats based on these max durations in hours
export const DEFAULT_MAX_DURATION_JOBS = 4;
export const DEFAULT_MAX_DURATION_PIPELINES = 24;

// Default values for pagination used in frontend
export const DEFAULT_PAGE_SIZE = 36;
